# /etc/default/kafka-broker
# {{ ansible_managed }}

ENABLE="yes"
export KAFKA_HEAP_OPTS="-Xmx{{ kafka.heap }} -Xms{{ kafka.heap }}"
export LOG_DIR="/var/log/kafka"
export KAFKA_LOG4J_OPTS="-Dlog4j.configuration=file:/etc/kafka/log4j.properties"
export JMX_PORT="{{ kafka.jmx_port }}"
